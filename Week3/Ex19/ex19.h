#ifndef EX_19_H
#define EX_19_H

#include <iostream>
#include <string>
#include <time.h>
#include <math.h>

using namespace std;

extern double ln2;					//Declare ln2 as a global variable
size_t bit_shift(size_t value);		//Declare function to find MSbit with bit shifts
size_t log_div(size_t value);		//Or with log division
size_t bin_search(size_t value);	//Or with binary search

#endif
